<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 
    NOTE: Most theme configuration is done in Compose (Theme.kt).
    These minimal XML styles are kept only for AndroidManifest compatibility.
    App uses 100% Jetpack Compose for UI - no XML layouts.
    -->

    <!-- Splash Screen Theme (required by AndroidManifest) -->
    <style name="Theme.App.Starting" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    </style>
</resources>