# ⛔ HANDOFF — 2026-07-22 — "Finish the app → clean build → commit to main"

> ## ✅ FINAL STATUS — 2026-07-22 Android/device closure
>
> `main` and `origin/main` are synchronized at
> [`0093e26`](https://github.com/ntoledo319/HELLDECK/commit/0093e2623df3a76b7c55c76278d7fb5297c34a87),
> with a clean worktree before this documentation-only update.
>
> - The normal Home CTA now starts its first round. The CI-only failure was a real production race:
>   `CardBuffer` existed but had no installed request, so `startRound(null)` could throw
>   `CardBuffer: No request set, cannot generate card`.
> - `CardBuffer` is now request-driven and owned by the ViewModel scope. Request changes invalidate
>   stale buffered/in-flight results, generation runs off the main thread, refill jobs terminate
>   when full, and teardown can await the engine before database/model replacement. Round starts are
>   serialized, duplicate taps are debounced, structured cancellation is preserved, abandoned
>   starts cannot commit UI state, and `ROUND` is entered only after generation and persistence.
> - Fresh local verification passed **155 JVM tests**, production APK assembly, instrumentation APK
>   assembly (including the prior D8 failure boundary), Ktlint, Detekt, Spotless, and Android lint
>   with zero findings. Four deterministic `CardBuffer` concurrency regressions cover first use,
>   game changes, stale prefetch, and superseded in-flight work.
> - GitHub is fully green: [Quality Gates](https://github.com/ntoledo319/HELLDECK/actions/runs/29958242132)
>   and [Legacy Android CI / Release](https://github.com/ntoledo319/HELLDECK/actions/runs/29958242050).
>   The archived reports prove **155/155 JVM tests** and **23/23 API 29 x86_64 device tests**, with
>   zero failures, errors, or skips. `homePrimaryActionStartsARound` passed on the emulator.
> - No CI or known first-round release blocker remains. Real human/device playtests, live
>   Stripe/Workers/domain/Play infrastructure, and signing/service credentials remain owner gates.
>   Room TTL/expiry, crew-memory persistence, and inherited legacy deprecation/compiler warnings
>   remain explicit roadmap debt rather than hidden completion claims.
>
> The original assignment below is retained as historical context; it is not an open checklist.

Written by the 2026-07-21/22 Opus session for the **next agent**. The owner's instruction this
session was, verbatim intent:

> Get the app done first. Then build/test here until we're free of errors **and warnings**, then
> commit + push all to git `main`. **Only — don't [you] do this. Leave a handoff for the next guy
> to do it, with loads of context from this session.**

So this is **your** job list. I (the prior agent) deliberately did **not** finish the app, did not
chase the last warnings, and did not make the final commit — I left it to you with full context.
Read this top-to-bottom, then read `HELLDECK2_HANDOFF.md` §3 (the 2026-07-21 paragraph) and
`NEXT_AGENT.md` for the deeper baseline.

> ## ✅ RESOLVED — 2026-07-22 completion pass
>
> This handoff's three-step assignment is complete. The historical diagnosis and instructions below
> are retained because they explain the failure boundary; they are no longer an open work list.
>
> - **Live proof:** one isolated local Worker, five strict WebSocket bots, depth 5, all five circles,
>   all three Scatter BOMB/BOOM loops, and **JUDGMENT in 419.3s**. The final Worker log contained only
>   expected HTTP/WebSocket access lines: zero alarm diagnostics, protocol errors, or runtime warnings.
> - **Timer root cause and repair:** Scatter's hidden fuse is durable and heartbeat-recoverable. The
>   RoomDO scheduler now separates strict logical deadlines from physical wakes guarded by 50ms,
>   preserves an already-armed earlier sentinel instead of moving it later under competing traffic,
>   suppresses nested re-arms during timer-driven reducer transitions, and force-arms one true
>   successor at every pump. Engine state and its timer index commit atomically.
> - **Entitlement integrity:** missing/invalid devices fail closed; free-night claims are atomic and
>   crash-idempotent per `ROOM:epoch`; rejected starts do not spend them; ledger uncertainty returns
>   retryable `ENTITLEMENT_UNAVAILABLE`; dev unlock requires exact `ENV=dev` plus a signing secret.
> - **Harness and release safety:** `pnpm smoke:live` has a 900s ceiling, strict socket/protocol
>   failures, Scatter watchdogs, exact completion invariants, heartbeats, elapsed phase telemetry,
>   and clean shutdown. Legacy CI is path-scoped; release jobs are manual/main-only/confirmed.
> - **Post-publication legacy CI repair:** the first GitHub push correctly ran Descent green, then
>   exposed pre-existing Android flavor drift. Both legacy workflows now use exact
>   `productionDebug` tasks and artifact paths; the Compose manifest conflict, ignored debug
>   keystore, empty-submodule CMake trap, and x86_64-emulator ABI mismatch are fixed. The local JDK
>   17 matrix now passes **155 Android unit tests**, dual-ABI APK assembly,
>   Detekt/Ktlint/Spotless, and baseline-enforced lint. GitHub additionally proves **23/23** tests
>   on its API 29 x86_64 emulator. Release signing fails closed and consumes an
>   environment-approved base64 keystore instead of silently producing an unsigned artifact.
> - **Verified head before publication:** engine **342** + server **67** + client **131** = **540 tests**,
>   recursive strict build, all **1,024** content cards through lint/dedup/stats, workflow validation,
>   frozen install, clean peer graph, zero-vulnerability full/production audits,
>   dependency/secret/diff checks, and the live proof above. `NEXT_AGENT.md`, `HELLDECK2_HANDOFF.md`,
>   `DESCENT_BUILD_SPEC.md`, and the onboarding proof carry the same current facts.
> - **Still genuinely open:** real human/device playtests; room TTL/expiry and crew memory; live
>   Stripe/Workers/domain/Play infrastructure and signing credentials. Those are product/owner gates, not a
>   continuation of this alarm task.

---

## 0. TL;DR — YOUR THREE STEPS

1. **Get the app "done"** = make it *confidently device-testable*. The one real code gap is: **prove a
   full live night reaches JUDGMENT** (my two live bot smokes stalled mid-scatter — see §2). Resolve
   that (it's most likely bot-harness pacing, but confirm it's not a live-alarm gap on module timers).
2. **Build + test in `descent/` until ZERO errors AND ZERO warnings** (§3). As of `7a533be` the build
   is already clean and **519 tests pass** — your job is to keep it clean through whatever you change
   and to hunt any *runtime* warnings during the live night.
3. **Then `commit push all git main`** (§5) — stage everything (including this handoff), commit with
   the required trailers, push to `origin/main`.

**Historical starting HEAD for the assignment below: `7a533be`.** See the final-status block above
for the current published head and verification record.

---

## 1. WHAT ALREADY LANDED THIS SESSION (do NOT redo — it's committed & pushed)

Commit **`7a533be` "Add host-unlock monetization (D-412/413) + descent CI"** is on `origin/main`.
It closed the single biggest gap ("MONETIZATION IS 0%"). The whole $9.99 host-unlock vertical is
built, tested (519 green), live-verified over the wire, and **taste-critic APPROVED**. Details:

**Server**
- `packages/server/src/entitle.ts` — device-bound **HMAC unlock** (sign/verify, `UNLOCK_SECRET`) +
  the `resolveEntitlement` decision (`entitled = unlocked || !freeNightUsed`).
- `packages/server/src/ledger-do.ts` — **`LedgerDO`**, one DO per host device
  (`idFromName(deviceToken)`), holds the single stateful bit: has this phone spent its one free
  night? Idempotent `/status` + `/consume-free`.
- `packages/server/src/stripe.ts` — minimal **test-mode** Checkout + session-verify (inline $9.99
  SKU, no Price object). Swapping `sk_test_`→`sk_live_` is the only change for prod.
- `packages/server/src/worker.ts` — routes `/api/entitle/{status,checkout,verify,dev-unlock}`;
  exports `LedgerDO`. **`dev-unlock` is non-prod only** — mints an unlock with no payment so the
  flow is testable without any Stripe key.
- `packages/server/src/room-do.ts` — **the key wiring**: entitlement is re-resolved against the host
  DEVICE at **every BEGIN** (not cached at room init), so the paywall lands on the device's *second*
  night whether that's a `resetNight` of the same room or a brand-new room. The free night is
  **charged only when a night actually starts** (a BEGIN the engine rejects never burns it). `dev`
  and `unlock` ride the host's own socket (`?dev=&unlock=`), never on the wire to other players.
- `packages/server/wrangler.toml` — `LEDGER` DO binding + migration `v1` now lists both DOs.

**Client**
- `packages/client/src/entitle.ts` — `hd:device` + `hd:unlock` in localStorage; `parseUnlockReturn`.
- `packages/client/src/screens/paywall.tsx` — the **UNHINGED** paywall overlay ("PAY THE TOLL — $9.99").
- `packages/client/src/screens/bits.tsx` — new inline-SVG **`Sigil`** atom (a sealed downward star;
  replaced a `⛧` text glyph that risked emoji/tofu on phones).
- `packages/client/src/net/ws.ts` — sends `dev`/`unlock` on connect (read fresh each reconnect).
- `packages/client/src/main.tsx` — opens the paywall on `NO_ENTITLEMENT`; finishes the Stripe return
  (verify → store unlock → clean reload so the socket reconnects entitled).

**Ops / docs also in `7a533be`**
- `.github/workflows/descent-ci.yml` — build + test + content-funnel CI for `descent/`.
- `.gitignore` — changed `.github/` → `.github/*` so new workflow files are actually tracked (the old
  rule silently kept `ci.yml` and new workflows untracked). Side effect: the pre-existing `ci.yml`
  got committed too.
- `descent/pnpm-workspace.yaml` — committed `allowBuilds: {esbuild, sharp, workerd: true}` so a fresh
  `pnpm install --frozen-lockfile` runs native build scripts **non-interactively** (this also kills
  the old "git checkout pnpm-workspace.yaml after every install" gotcha — that gotcha is GONE now).
- Docs synced: `HELLDECK2_HANDOFF.md` §3 (2026-07-21 paragraph is authoritative), `DESCENT_BUILD_SPEC.md`
  Part 12 (D-412/D-413 checked), `NEXT_AGENT.md`.

**Tests added:** `packages/server/test/entitle.test.ts` (HMAC, decision, ledger idempotency) and an
"entitlement at BEGIN" block in `packages/server/test/protocol.test.ts` (free-night-is-per-device
cross-room, paid-always-plays, forged-token-rejected, rejected-BEGIN-doesn't-burn). Plus
`packages/client/src/entitle.test.ts`.

---

## 2. "GET THE APP DONE" — THE ACTUAL OPEN ITEM (the scatter live-night stall)

Everything compiles and the **deterministic** full-night test (`packages/engine/test/night.test.ts`
+ the server "full mixed night" test) already plays **all 9 games to JUDGMENT** — that's the
authoritative correctness proof and it's green.

**But I could not get a clean FULL LIVE night this session.** Two `wrangler dev` bot smokes
(`HELLDECK_N=5 HELLDECK_DEPTH=5`) both **timed out mid-scatter at circle 2–3 of 5**:
- Run 1 completed roast (c0) + overunder (c1), entered scatter (c2), finished one BOMB→BOOM→REVEAL,
  stalled on the 2nd bomb.
- Run 2 completed roast + overunder, entered scatter, **froze at the first BOMB (epoch 26)** until the
  480 s harness timeout.

**Why this is probably NOT a regression from `7a533be`:** my changes touch only the BEGIN/entitlement
path — nothing in the game/timer/alarm code. Roast + overunder timers (LADDER transitions, reveal
holds) fired fine both runs. Scatter is inherently slow: `packages/engine/src/games/scatter.ts` runs
**3 bombs/circle**, each with a **hidden 15–45 s fuse** (`FUSE_MIN_MS=15_000`, `FUSE_SPAN_MS=30_000`)
and a **null phase deadline** (the fuse is the secret — no countdown on the wire). Bots send **no
input during BOMB** (it self-advances on the fuse). So a scatter-heavy arc + overunder's multi-round
flow simply runs longer than the harness's `TIMEOUT_MS = 480_000` (`test/bots.ts:28`).

**Your job — pick the cheapest sufficient proof, in order:**
1. **Raise the bot timeout** and re-run: bump `test/bots.ts:28` `TIMEOUT_MS` for depth-5 to e.g.
   `900_000`, then `HELLDECK_N=5 HELLDECK_DEPTH=5 npx tsx test/bots.ts` against a running
   `wrangler dev`. If it reaches JUDGMENT → it was pure pacing; you're done, revert the timeout bump.
2. **If it still hangs at BOMB** (no BOOM after ~60 s), that's a real live-alarm gap worth fixing
   **before any human playtest** (a real night would stall the same way — BOMB→BOOM needs no human
   input, only the fuse alarm). Investigate: is the scatter fuse a core `SCHEDULE` timer (lands in
   `RoomDO.timers`, covered by the self-heal on any inbound frame) or an engine **`moduleTimer`**
   (fired by the core alarm / DESCEND, maybe *not* covered by the `webSocketMessage` self-heal pump)?
   See `room-do.ts` `pump()` + the trailing self-heal, and `engine.ts` `moduleTimers`/`handleTimer`.
   `wrangler dev --local` has known flaky-alarm behavior (the `ALARM_EPSILON_MS` comments) — the
   self-heal exists exactly for this, so confirm it reaches module-scheduled fuse timers.
3. The prior session (pre-monetization) *did* verify a depth-5 live night to JUDGMENT in 360 s — so a
   clean full live night is achievable; you're re-confirming it on this build.

**Live-run recipe (this environment — loopback works, real WAN is sandboxed):**
```
cd descent/packages/server
CI=true node_modules/.bin/wrangler dev --port 8787 --local --var UNLOCK_SECRET:devsecret123 --var ENV:dev
# in another shell, wait for "Ready on http://localhost:8787", then:
HELLDECK_N=5 HELLDECK_DEPTH=5 npx tsx test/bots.ts     # PASS = "JUDGMENT reached in …s"
```
Kill both when done (`pkill -f "wrangler dev"; pkill -f "tsx test/bots"`).

**Human playtest (D-128/D-138) is the true product gate and is OWNER-GATED — not yours to close.**
For a real on-phones LAN test (owner action): build the client, run `wrangler dev --ip 0.0.0.0`
(with `UNLOCK_SECRET` + `ENV=dev` so the in-app **dev-unlock** button lets the 2nd night through
without payment), everyone opens `http://<laptop-LAN-IP>:8787`. The client WS uses `location.host`,
so LAN IPs work as-is.

---

## 3. "FREE OF ERRORS AND WARNINGS" — WHAT THAT MEANS HERE

There is **no eslint/prettier gate in `descent/`** — the quality bar is `tsc --strict` (each package's
`build` is `tsc --noEmit`) + `vite build` + `vitest`. As of `7a533be` **all of it is clean: no build
warnings, no type errors, 519 tests pass.**

**Build/test recipe — run from `descent/`, NEVER the repo root** (the root has a frozen legacy `webui`
package whose test script runs a failing e2e suite):
```
cd descent
CI=true pnpm -r --config.verifyDepsBeforeRun=false build
CI=true pnpm -r --config.verifyDepsBeforeRun=false test
```
(The `verifyDepsBeforeRun=false` flag skips pnpm's pre-run deps re-check, which otherwise aborts
"no TTY" in this sandbox. `node_modules` is already installed; `allowBuilds` is now committed so a
real `pnpm install --frozen-lockfile` also works non-interactively.)

**On "warnings" specifically:**
- The three `// eslint-disable-next-line react-hooks/exhaustive-deps` in `main.tsx:234` and
  `bits.tsx:75,112` are **intentional** (deadline-driven effects that must not re-fire on every dep
  change). Two are pre-existing; one (`main.tsx`, the Stripe-return effect) I added. If the owner
  wants an *eslint* gate added, these are deliberate suppressions, not bugs to chase — but document
  them if you wire eslint.
- Hunt **runtime** console warnings during the live night (Preact key warnings, WS errors, etc.) —
  those won't show up in `tsc`/`vitest`.
- If you touch cards, the content funnel must stay green:
  `python3 content/tools/{lint_deck,dedup_skeletons,deck_stats}.py content/decks/*.json` (dedup is
  authoritative across ALL decks; if you change deck sizes, update the count assertion in
  `packages/server/test/content.test.ts`).

---

## 4. TASTE + ENGINE LAW (don't regress these while "finishing")

- **Taste is enforced adversarially.** The paywall was reviewed by the `taste-critic` subagent and
  APPROVED as DISTINCTIVE — but only after it caught an `aria-label="Unlock every night"` leaking
  banned SaaS vocabulary into the *accessible name*. Lesson: the voice (UNHINGED, never WHOLESOME)
  must hold into a11y labels too, not just visible copy. Run `taste-critic`/`taste-auditor` on any new
  UI/copy. Their memory: `descent/packages/client/.claude/agent-memory/taste-critic/` (paywall = the
  first clean approval on the ledger) and `descent/content/.claude/agent-memory/taste-auditor/`.
- **Engine purity:** `packages/engine` has no `Date.now`/`Math.random`/IO — time is `event.at`,
  randomness is seeded `rng(seed)`. `packages/server/src/redact.ts` is the ONLY path to the wire;
  secrets go in `NEVER_SERIALIZE`. Device/unlock tokens live on the socket **Attachment**, never in
  `RoomState`, so they never touch redaction — keep it that way.

---

## 5. THEN: COMMIT + PUSH (the final step the owner wants YOU to do)

Once the app is "done" and the build/tests are clean of errors **and** warnings:
```
git -C /home/nick/Development/active/HELLDECK add -A
git -C /home/nick/Development/active/HELLDECK commit -F <your message file>
git -C /home/nick/Development/active/HELLDECK push origin main
```
`main` is the intended branch (the owner said so explicitly). **Commit messages MUST end with:**
```
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: <your session URL>
```
Stage **all** (`add -A`) — including this handoff and any doc updates — that's what "commit push all"
means. No active git hooks, so the commit won't run pre-commit checks. Verify `HEAD == origin/main`
after pushing.

---

## 6. STALE-CONTEXT WARNINGS (things that will mislead you if you trust them blindly)

- **Auto-memory is STALE.** `~/.claude/projects/…/memory/helldeck-2-descent-pivot.md` reverted this
  session to its pre-monetization state — it still says "engine 310, server 30, client 87 = 427 pass"
  and "everything left is owner-gated … M6 domain + Stripe." **That is now wrong** — monetization is
  built (see §1) and tests are 519. Trust `HELLDECK2_HANDOFF.md` §3 (2026-07-21) and this file instead.
  (I did not re-edit the memory because the revert was flagged intentional; correct it if you can.)
- **`DESCENT_BUILD_SPEC.md` Part 12** was badly stale before this session; D-412/D-413 are now checked
  and accurate, but other boxes may still under-report reality — cross-check against `HELLDECK2_HANDOFF.md`
  §3 before believing "not done."
- **Owner-gated, do NOT attempt:** `STRIPE_SECRET` live key, real human playtests, Workers Paid
  deploy, domain purchase, Play Console / Android shell signing. Say plainly what's stubbed vs real.

---

## 7. FASTEST PATH FOR YOU

1. `git -C /home/nick/Development/active/HELLDECK log --oneline -3` + `status` — confirm you're at
   `7a533be`, clean.
2. `cd descent`; run the build + test recipe (§3) — confirm 519 green, no warnings (your baseline).
3. Do §2 step 1 (raise bot timeout, run a live night to JUDGMENT). If it passes → the app is "done"
   for device-test purposes; if it hangs at BOMB → fix the module-timer/self-heal gap first.
4. Re-run build + test; ensure still clean of errors **and** warnings; check for runtime warnings.
5. Commit + push all to `main` (§5) with the trailers.
6. Update `HELLDECK2_HANDOFF.md` §3, this file, and (if you can) the auto-memory, so the *next* next
   agent picks up cold.
